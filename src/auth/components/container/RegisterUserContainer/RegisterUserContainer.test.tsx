import { render } from '@testing-library/react';

describe('RegisterUserContainer', () => {
  /** *
   *    ui state 값이
   *    - 회원가입 성공일 경우 성공 메시지를 보여주고 로그인 페이지로 이동한다.
   *    - 회원가입 실패일 경우 에러 메시지를 보여준다.
   */
});
